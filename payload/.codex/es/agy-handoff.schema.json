{
  "type": "object",
  "properties": {
    "version": {
      "type": "integer",
      "minimum": 2,
      "maximum": 2
    },
    "status": {
      "type": "string",
      "enum": [
        "ready",
        "partial",
        "not_found",
        "blocked"
      ],
      "description": "ready: primary evidence and no unresolved questions. partial: primary evidence plus unresolved questions. not_found: empty primary/related and explain the missing evidence in unresolved. blocked: empty primary/related and explain the obstacle in unresolved."
    },
    "primary": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "path": {
            "type": "string",
            "maxLength": 400,
            "minLength": 1
          },
          "start": {
            "type": "integer",
            "minimum": 1
          },
          "end": {
            "type": "integer",
            "minimum": 1,
            "description": "Inclusive end line, at least start."
          },
          "symbol": {
            "type": "string",
            "maxLength": 160
          },
          "evidence": {
            "type": "string",
            "maxLength": 220,
            "minLength": 1,
            "description": "Explain why this location answers the question. Do not repeat source text; the host attaches the original lines."
          }
        },
        "required": [
          "path",
          "start",
          "end",
          "symbol",
          "evidence"
        ],
        "additionalProperties": false
      },
      "maxItems": 3
    },
    "related": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "path": {
            "type": "string",
            "maxLength": 400,
            "minLength": 1
          },
          "start": {
            "type": "integer",
            "minimum": 1
          },
          "end": {
            "type": "integer",
            "minimum": 1,
            "description": "Inclusive end line, at least start."
          },
          "symbol": {
            "type": "string",
            "maxLength": 160
          },
          "evidence": {
            "type": "string",
            "maxLength": 220,
            "minLength": 1,
            "description": "Explain why this location answers the question. Do not repeat source text; the host attaches the original lines."
          }
        },
        "required": [
          "path",
          "start",
          "end",
          "symbol",
          "evidence"
        ],
        "additionalProperties": false
      },
      "maxItems": 2
    },
    "unresolved": {
      "type": "array",
      "items": {
        "type": "string",
        "maxLength": 200,
        "minLength": 1
      },
      "maxItems": 3
    }
  },
  "required": [
    "version",
    "status",
    "primary",
    "related",
    "unresolved"
  ],
  "additionalProperties": false,
  "description": "Source evidence report. Cite only the smallest complete ranges needed for the question. Ranges must exist in the supplied files, be 1-based and inclusive, and not overlap within a file across primary/related. Use normalized repository-relative POSIX paths. Use plain single-line strings without control characters."
}
